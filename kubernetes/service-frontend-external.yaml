apiVersion: v1
kind: Service
metadata:
  name: ble-sensors-mqtt-frontend-external
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: ble-sensors-mqtt
  ports:
    - name: frontend
      port: 8080
      targetPort: frontend
      protocol: TCP
