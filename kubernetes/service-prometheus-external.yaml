apiVersion: v1
kind: Service
metadata: {name: ble-sensors-mqtt-prometheus-external}
spec:
  type: LoadBalancer
  selector: {app.kubernetes.io/name: ble-sensors-mqtt}
  ports:
    - {name: prometheus, port: 9105, targetPort: prometheus, protocol: TCP}
