apiVersion: v1
kind: Service
metadata: {name: ble-sensors-mqtt-snmp-external}
spec:
  type: LoadBalancer
  selector: {app.kubernetes.io/name: ble-sensors-mqtt}
  ports:
    - {name: snmp, port: 1161, targetPort: snmp, protocol: UDP}
